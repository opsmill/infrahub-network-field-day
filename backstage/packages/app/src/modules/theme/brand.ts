/**
 * Otter-net's colours, sampled from the logo in `docs/docs/media/otternet.png`
 * rather than guessed, so the portal and the brand actually match.
 */
export const otternet = {
  /** The otter's body and the wordmark. */
  navy: '#1a3545',
  navyDeep: '#0c2735',
  /** The waves and the "INTERNET" strapline. */
  teal: '#2ca6ab',
  /** Dark enough to read as text and links on white. */
  tealInk: '#0f7d8c',
  /** The brighter arcs on the right of the mark. */
  cyan: '#45bebe',
  /** The wifi arcs on the left of the mark. */
  mint: '#3ecf8e',
} as const;
